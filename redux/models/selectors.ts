export const selectModels = (state: any) => state.model.models;

export const selectIsLoading = (state: any) => state.model.isLoading;

export const selectCurrentModel = (state: any) => state.model.currentModel;

export const selectModel = (state: any) => state.model.model;
